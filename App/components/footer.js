import { View } from "react-native/types";
import { useTailwind } from "tailwind-rn/dist";

const footer = () => {
    const tailwind = useTailwind();
    return (
        <View>
            {/* Import Social media Icons into the images folder 
            And display them in the footer bar with embedded Links to
            NSBE social media pages */}
        </View>
    )
}
export default footer;